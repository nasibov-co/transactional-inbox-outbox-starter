# Transactional Inbox/Outbox JDBC Starter

Use `io.github.fnasibov:transactional-inbox-outbox-starter-jdbc` in blocking Spring Data JDBC applications.
The shared auto-configuration detects JDBC infrastructure and creates `JdbcEventRepository`.
